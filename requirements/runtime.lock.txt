# Exact, reproducible dependency set for `pip install -e ".[runtime]"`.
#
# This is the minimum needed to run the chamber protocol (tcp/), PID/BO
# helpers (deepvac.pid, deepvac.metrics), and inference with an already
# trained GRU/LSTM checkpoint (deepvac.mpc, gru/mpc_gru.py, lstm/mpc_lstm.py,
# optimization/*_experiment.py). It does NOT include matplotlib, mlflow, or
# optuna -- those are only needed to train new models (see training.lock.txt)
# or to plot/analyze results (see visualization.lock.txt).
#
# Regenerate by installing `.[runtime]` into a clean environment and running
# `pip freeze`, or see requirements/README.md for the closure method used to
# derive this from the full validated environment.
filelock==3.29.1
fsspec==2026.4.0
jinja2==3.1.6
joblib==1.5.3
markupsafe==3.0.3
mpmath==1.3.0
networkx==3.4.2
numpy==2.2.6
pandas==2.3.3
python-dateutil==2.9.0.post0
pytz==2025.2
scikit-learn==1.7.2
scipy==1.15.3
setuptools==81.0.0
six==1.17.0
sympy==1.14.0
threadpoolctl==3.6.0
torch==2.12.0
typing-extensions==4.15.0
tzdata==2025.3
